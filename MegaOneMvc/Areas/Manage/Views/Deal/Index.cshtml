@using MegaOneMvc.ViewModels.Deal
@using MegaOneMvc.ViewModels.Food
@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

@model IEnumerable<GetDealVM>

<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Inverse table</h4>
            <p class="card-description">
                <a type="button" asp-action="CreateDeal" asp-controller="Deal" class="btn btn-success">Create</a>
            </p>
            <div class="table-responsive pt-3">
                <table class="table table-dark">
                    <thead>
                        <tr>
                            <th>
                                #
                            </th>
                            <th>
                                Name
                            </th>
                            <th>
                                Description
                            </th>
                            <th>
                                Price
                            </th>
                            
                            <th>
                                Delete
                            </th>
                            <th>
                                Update
                            </th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>

                                <td>
                                    @item.Id
                                </td>
                                <td>
                                    @item.DealName
                                </td>
                                <td>
                                    @item.Description
                                </td>
                                <td>
                                    @item.Price
                                </td>
                                
                                
                                <td>
                                    <a type="button" asp-route-id="@item.Id" asp-action="DeleteDeal" asp-controller="Deal" class="btn btn-danger">Delete</a>
                                </td>
                                <td>
                                    <a type="button" asp-route-id="@item.Id" asp-action="UpdateDeal" asp-controller="Deal" class="btn btn-warning">Update</a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>